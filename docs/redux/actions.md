`BUILD_TASK`
`{ description }`

`ADD_TASK`
`{ id, description, created }`

`FINISH_TASK`
`{ id }`

`ADD_FINISHED_TASK`
{ id, description, finished }`

`REMOVE_TASK`
`{ id }`

`INCREMENT_SCORE`

`DECREMENT_SCORE`

`EXPIRE_TASKS`
`{ time }`
